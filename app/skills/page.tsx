import type {Metadata} from 'next';import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {getProfessionalContent,publishedByOrder} from '@/lib/cms/publicProfessional';import styles from '../profile-pages.module.css';
export const dynamic='force-dynamic';export const metadata:Metadata={title:'Skills',description:'Barnabas Mikel’s software engineering, AI, automation and systems-integration skills with supporting project evidence.'};
export default async function SkillsPage(){const content=await getProfessionalContent();const builds=publishedByOrder(content.capabilities),skills=publishedByOrder(content.skillGroups),proof=publishedByOrder(content.proofLinks);return <main className={styles.page}><section className={styles.hero}><span className="eyebrow">TECHNICAL SKILLS</span><h1>Engineering behind<br/>the outcomes.</h1><p>Technologies and capabilities connected to working software, professional experience and continued engineering practice.</p><div className={styles.heroActions}><Link className="button black" href="/projects">See project proof <ArrowRight/></Link><Link className="button" href="/services">View services <ArrowRight/></Link></div></section><section className={styles.section}><div className={styles.sectionHead}><div><span className="eyebrow">CAPABILITIES</span><h2>What I can build and engineer.</h2></div><p>Technology is useful when it creates clear, maintainable systems that solve a real problem.</p></div><div className={styles.buildGrid}>{builds.map(item=><article className={styles.buildCard} key={item.title}><small>{item.label}</small><h3>{item.title}</h3><p>{item.summary}</p></article>)}</div></section><section className={styles.section}><div className={styles.sectionHead}><div><span className="eyebrow">TOOLS & TECHNOLOGIES</span><h2>Skills I know and apply.</h2></div><p>Grouped by the kind of engineering work they support—not presented as a disconnected logo wall.</p></div>{skills.map(group=><div className={styles.skillGroup} key={group.title}><h3>{group.title}</h3><div className={styles.chips}>{group.skills.map(skill=><span key={skill}>{skill}</span>)}</div></div>)}</section><section className={styles.section}><div className={styles.sectionHead}><div><span className="eyebrow">PROOF MAP</span><h2>Follow each claim to the work.</h2></div><p>Projects provide deeper context: problem, decisions, implementation and lessons.</p></div><div className={styles.proofGrid}>{proof.map(item=><Link className={styles.proofCard} href={item.href} key={`${item.title}-${item.href}`}><small>{item.label}</small><div><h3>{item.title}</h3><p>{item.summary}</p></div><b>View evidence →</b></Link>)}</div></section></main>}
